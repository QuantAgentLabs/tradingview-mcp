{
  "mcpServers": {
    "tradingview": {
      "type": "http",
      "url": "http://127.0.0.1:3000/mcp",
      "note": "Persistent OrbStack MCP endpoint. Start it with `npm run tvSetup` or `npm run tvUp` after building the image."
    }
  }
}
